1、redis是一款开源的nosql（not only sql），非关系型数据库，以key--value的形式进行存储数据
2、redis的特点
   2-1：reids读写速度非常快，应为redis是将数据库的的数据线存储到内存，然后再进行操作，所以读写速度特别快


3、redis数据结构
   redis有五种数据结构
   String
   list
   hash
   set
   sort set 会给元素一个分数，用于排序


4redis为什么高效
  redis是将数据存储到了本地的内存，因而数据查询较快

4、redis持久化
   由于所有数据保存到内存上，所以对数据异步更新到磁盘上，redis提供了一些策略

   aof：将数据库所有修改操作记录下来，当redis重启时，会通过重新执行文件中保存的写命令在内存
        中重建整个数据库的内容。
        aof优势在于:就算出问题了,最多丢失1秒内的更新数据，比较安全
        aof的劣势:
        aof文件的体积可能会很大(可能比快照文件还大),另一方面,系统重启的时候回从aof里读命令,
        如果aof文件太大,读命令也就要还很久

   RDB：将修改过的数据保存下来，每隔一段时间将数据更新磁盘，rbd文件紧凑，适合备份恢复，但是他是每隔一段时间（如30s）开启一个folk（）
        往数据库同步一次数据，如果数据库down机，可能会丢失30s的数据，在很多时候是不可忍受的，除此之外，数据量过大，folk可能
        导致客户端停止处理请求
   
5、redis集群
   redis
   主从哨兵模式：
   哨兵：可做多个，也是一个redis，服务监控和当某一个redis死掉的时候选举出主服务器
   主：向外提供写服务
   从：向外提供读的服务

   高可用集群
   