1、jpa是java5.0提供的应用程序统一使用的一种访问持久层的一种规范,具体实现由厂商实现，orm框架均实现这个
   框架，因此，他很适合移植，使用jdk5.0的注解来实现，使用面向对象的查询（jsql）

2、
@Table（name=“”）映射到数据库时表的名称与那么的值相同，数据库中表不存在，会创建一个数据库表，
@Entity 表示这是一个实体类，并且参与数据库映射
@Id标注映射到数据库的主键，放在get方法上面
@GeneratedValue 主键的生成策略，可进行设置